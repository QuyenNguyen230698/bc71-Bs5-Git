

Repository local (trong máy user)

Repository remote (trên github)

Các bước tạo liên kết dự án chung bằng Github

git init : tạo Repository local (mỗi dự án làm 1 lần)

git remote add origin https://github.com/QuyenNguyen230698/bc71-Bs5-Git.git : liên kết repos local và repo remote

push gồm 3 bước (chọn, xác nhận, gửi)
     Step1: git add -A
     Step2: git commit -m " nội dung của commit"
     Step3: git push -u origin master (lần đầu) / git push (lần 2 trở đi)

git log : hiển thị lịch sử commit

* tạo nhánh và code cho nhánh
git checkout -b "tên nhánh" (branch) : tạo nhánh khác song song với master "branch là tên nhánh"
git checkout branch : kiểm tra bạn đang code push ở nhánh (branch) nào ở GitHub
git checkout branch (nhánh) : chuyển về nhánh (branch) bạn muốn 

* Đứng ở master kéo nhánh (branch) về master

git merge master (hoặc nhánh "branch") : dùng để gộp code từ nhánh phụ về master

git clone https://github.com/quangsiDev/bc71-bs5-git.git . : tải và tạo source code bằng link github 

git pull : cập nhật remote của ng khác về local (chỉ ảnh hưởng đến nhánh hiện tại)





git init : khởi tạo repo local

git remote add origin url : kết nối đường dẫn url từ local lên remote ( github )

git remote show origin : hiển thị đường dẫn đang được kết nối


git push : đưa source từ local lên remote
  step 1: git add -A
  step 2: git commit -m " description "
  step 3: git push -u origin master (đẩy lần đầu / master là tên nhánh )
          git push ( lần 2 trở đi, sau khi trên github đã có nhánh master)

git log : hiển thị lịch sử commit

git checkout -b branch : tạo nhánh mới ( branch là tên nhánh / nhớ comit )

git checkout branch : chuyển nhánh ( nhớ commit)

git merge login ( đem code từ nhánh login về nhánh hiện tại)

git clone https://github.com/quangsiDev/bc71-bs5-git.git .

git pull : lấy code từ remote về local ( chỉ ảnh hưởng đến nhánh hiện tại  )